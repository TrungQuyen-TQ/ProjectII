services:
  sqlserver:
    build: .
    container_name: sqlserver
    environment:
      SA_PASSWORD: "Sa123456!"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - ./sql:/docker-entrypoint-initdb.d
